import { DEFAULT_RISK_WEIGHTING } from '../utils/risk.js';

export const initialRiskWeights = {
  low: DEFAULT_RISK_WEIGHTING.low,
  medium: DEFAULT_RISK_WEIGHTING.medium,
  high: DEFAULT_RISK_WEIGHTING.high
};
